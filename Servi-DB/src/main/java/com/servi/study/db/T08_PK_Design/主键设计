一、一共能表示多少种数据：2的16次幂再20次幂条数据
1、主键的大小是char(20)
2、以Mysql为例，char(1) = 2字节 = 2*8 = 16位
3、每位可以表示0或者1，两种。所以char(1)=2的16次幂种数据；
4、一共能表示2的16次幂再20次幂条数据；
====================================
二、人为限定char(1)的取值范围，0-9,A-Z;功36种;
最终的数据量为：36的20次幂条数据；
====================================================
三、20位主键的前4位，用来表示固定值；
char(4)就是，4*16=64位；也就是2的64次幂种数据；
====================================================
四、

