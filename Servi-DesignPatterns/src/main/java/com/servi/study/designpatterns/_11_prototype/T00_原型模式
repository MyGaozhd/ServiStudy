1、原型模式如果不实现Cloneable接口，在调用clone()方法时，抛出CloneNotSupportedException
2、Cloneable 是一个标识接口，并没有抽象方法，用来告诉jvm此对象可以被拷贝。
3、调用clone()，方法实际上是内存中二进制流的拷贝，比new一个对象性能好很多。
4、调用clone()生成的新对象是，不会调用类的构造方法。
5、默认clone方法时浅拷贝。浅拷贝只拷贝对象本身，对象内部字段仍然指向原始对象内部字段的地址。
6、实现深拷贝，需要修改clone方法，将新生成的对象内部字段重新赋值
7、如果类的字段有final修饰符，只能用浅拷贝。不能使用深拷贝。